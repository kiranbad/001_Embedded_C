/*3. Pointer to Pointer (Double Pointer)
Goal: Understand how to use double pointers for indirect referencing and dynamic memory management.
Activity:
1. Declare a double pointer and use it to dynamically allocate memory for a single variable or an array.
2. Access and modify the allocated memory via the double pointer.
3. Pass double pointers to functions to dynamically allocate or modify memory inside the function.*/
#include<stdio.h>
#include<stdlib.h>
int main()
{
    int **ptr;
}